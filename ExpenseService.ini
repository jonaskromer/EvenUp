ExpenseService
   ↓
ValidationDecorator
   ↓
BudgetDecorator
   ↓
CurrencyConversionDecorator
   ↓
FeeDecorator
   ↓
CoreExpenseService


Controller/TUI/REST
        ↓
   EvenUpFacade  ← Facade Pattern
        ↓
  ┌───────────────┐
  │ Domain Engine │ ← Decorator layers (validation, fees, budget, etc.)
  └───────────────┘
        ↓
  Repositories     ← Adapter Pattern (DB, JSON, etc.)
